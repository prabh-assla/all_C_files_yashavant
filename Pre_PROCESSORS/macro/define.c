#include <stdio.h>

#define PRO printf("can be 11.\n");
#define dune scanf("%d", &a);
#define straw (rate>10 AND rate<20)
#define AND &&
#define rate 11

int main(){
    if(straw)
    PRO
    else
    printf("life is a joke.\n");
    int a;
    printf("type a no. -->\t");
    dune
    printf("%d has been typed.\n", a);
return 0;
}